// import { database } from "../../firebase/fire";
// import TableAction from "./TableAction";

export default class TableEffect {
  // static syncTable({ restaurantId, tableNumber }) {
  //   database
  //     .ref(`${restaurantId.trim()}/tables/${tableNumber}`)
  //     .on("value", (snap) => {
  //       TableAction.stateUpdate(snap.val());
  //     });
  // }
}
